if has('nvim')
    set runtimepath+=~/.vim
endif

source ~/.vim/myvimrc
"source ~/.vim/macvimrc
source ~/.vim/ubunturc
if has("gui_running")
    source ~/.vim/gvimrc
endif
"source ~/.vim/androidrc
"source ~/.vim/ignitionrc
"source ~/.vim/ignition-ps4.vimrc
"source ~/.vim/windows.vimrc

if exists("g:neovide")
    " Put anything you want to happen only in Neovide here
endif
