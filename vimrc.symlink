set runtimepath+=~/.vim " for nvim

source ~/.vim/myvimrc
"source ~/.vim/macvimrc
source ~/.vim/ubunturc
if has("gui_running")
    source ~/.vim/gvimrc
    if exists("g:neovide")
        colorscheme github_dark_high_contrast
    endif
endif
"source ~/.vim/androidrc
"source ~/.vim/ignitionrc
"source ~/.vim/ignition-ps4.vimrc
"source ~/.vim/windows.vimrc
