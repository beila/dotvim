# dotvim (archived)

This repo has been merged into [dotfiles](https://github.com/beila/dotfiles) under `nvim.configsymlink/` via `git subtree add`.

Full commit history is preserved in the dotfiles repo.
